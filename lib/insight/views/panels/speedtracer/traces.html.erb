<% unless advice.empty? %>
  <p>Warning:</p>
  <ul>
    <% advice.each do |item| %>
      <li><%= item %></li>
    <% end %>
  </ul>
<% end %>
<dl>
  <dt><%= trace.uuid %> - <%= trace.start.strftime("%H:%M:%S") %></dt>
  <dd><%= trace.to_html %></dd>
</dl>
